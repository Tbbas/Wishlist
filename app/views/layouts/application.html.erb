<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
<link href="https://fonts.googleapis.com/css?family=Anton|Lora|Mountains+of+Christmas" rel="stylesheet">    <%= csrf_meta_tags %>
  </head>
  <body>
    <ul class="dropdown menu " data-responsive-menu="drilldown medium-dropdown" style="width: 300px;">
  <li>
    <a href="#" class="menuColor"><i class="fa fa-bars" aria-hidden="true"></i></a>
    <ul class=" class="" vertical menu ">
      <li>
          <% if admin_signed_in? %>
          <li>
            <%= link_to('Logout', destroy_admin_session_path, :method => :delete) %>
          </li>
          <% else %>
          <li>
            <%= link_to('Login', new_admin_session_path)  %>
          </li>
          <% end %>
      </li>
    </ul>
  </li>
  <li>
</ul>
    <div class="header"><h1 class="title"><a href="/" class="menuColor">WishList</a><span class="version">Alpha</span></h1></div>
    <div class="container">
    <%= yield %>
  </div>

  </body>
</html>
